#flashcards/Java

在 Java 中，像 `import java.util.Scanner;` 这样直接指明被导入类的全名的导入方式被称为什么？ :: 特定导入。

在 Java 中，若要使用通配符导入 `java.util` 包下的所有类，完整的导入语句应该怎么写？ :: `import java.util.*;` （注意：必须包含末尾的英文分号）。

如果 `input` 是一个已初始化的 `Scanner` 对象，要获取用户输入的 `int` 类型数据，应该调用哪个完整的方法？ :: `input.nextInt()` （注意：作为方法调用，必须带有括号）。