---
cards-deck: code::数据结构
---

#flashcards/数据结构

空串（Empty String）与空格串（Space String）的区别是什么？ :: 空串是长度为 $0$ 的无字符串；空格串是由一个或多个空格组成的串，长度为其包含的空格字符数量。
^1776413783972

串的基本操作与普通线性表的基本操作在操作对象上有什么根本区别？ :: 线性表操作多以“单个元素”为对象，而串的操作通常以“串的整体”（如比较、复制、查找子串）作为操作对象。
^1776413783975

串的块链存储（Block Chain Storage of Strings） #card

- 核心思想：使用链表存储串。为了提高“存储密度”，每个节点内部存放多个字符。
- 填充规则：如果最后一个节点未被占满，通常补上特殊的填充符号（如“#”）。
^1776413783977

在 BF（暴力匹配）算法中，若主串指针 $i$ 与模式串指针 $j$ 发生失配，主串指针 $i$ 的回溯公式是什么？ :: $i = i - j + 2$（假设指针从 $1$ 开始）。
^1776413783979

设主串长度为 $n$，模式串长度为 $m$，BF 算法的最好和最坏时间复杂度分别是多少？ :: 最好情况：$O(n+m)$；最坏情况：$O(n \times m)$。
^1776413783981

KMP 算法相比 BF 算法的核心优化点是什么？ :: 当发生失配时，主串指针 $i$ 不回溯（保持不变），仅将模式串向右滑动，模式串指针 $j$ 退回到 $next[j]$ 的位置重新比较。
^1776413783983

设主串长度为 $n$，模式串长度为 $m$，KMP 算法的时间复杂度是多少？ :: $O(n+m)$
^1776413783985

KMP 算法中 $next[j]$ 数组的本质含义（计算规则）是什么？ :: 当模式串第 $j$ 个字符失配时，下一次重新比较的字符位置。其值为模式串从 $1$ 到 $j-1$ 构成的子串中，最长相同真前缀和真后缀的长度 $+ 1$（若无则置为 $1$，当 $j=1$ 时置为 $0$）。
^1776413783987

KMP 算法的优化中，计算 $nextval[j]$ 的修正规则是什么？ :: 若当前字符与它 $next$ 位置的字符相同，则它的 $nextval$ 等于它 $next$ 位置字符的 $nextval$；如果不等，则它的 $nextval$ 就是它的 $next$ 值。
^1776413783989

在病毒感染检测中，由于病毒 DNA 是环状的，如何将其转化为线性模式串进行匹配？ :: 将长度为 $m$ 的病毒 DNA 序列扩大为 $2m$（连续存储两次），然后依次截取长度为 $m$ 的线性子串作为模式串，再去与主串进行匹配。
^1776413783991
