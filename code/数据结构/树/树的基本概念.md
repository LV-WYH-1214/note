---
cards-deck: code::数据结构
---

#flashcards/数据结构

**满二叉树** / 完美二叉树（Perfect Binary Tree） #card

- 定义：一棵深度为 $k$ 且有 $2^k-1$ 个结点的树称为满二叉树。
- 每一层的节点数都是最大节点数。
- 所有分支结点都存在左右子树，且所有叶结点都在同一层上。
^1776914986835

深度为 $k$ 的二叉树最大结点数是多少？ :: $2^k - 1$<br><br>**附：证明过程（无需背诵，理解即可）**<br>由性质1可知每一层最大结点数为 $2^{i-1}$，因此有：深度为 $k$ 的二叉树最大结点数 $= \sum_{i=1}^{k} 2^{i-1} = 2^k - 1$
^1776914986837

对于二叉树 $T$，设 $n_0$ 为叶结点个数，$n_2$ 为度为2的结点个数，它们之间的恒等关系式是什么？ :: $n_0 = n_2 + 1$<br><br>**附：证明过程（无需背诵，理解即可）**<br>1. 结点总数 $n = n_0 + n_1 + n_2$<br>2. 边总数（除根节点外每个节点都有一条入边） $n - 1 = n_1 + 2n_2$<br>将两式相减即得：$n_0 = n_2 + 1$
^1776914986838

完全二叉树（Complete Binary Tree） #card

- 树中第 $1 \sim k-1$ 层与完美（满）二叉树一致。
- 第 $k$ 层全部为叶结点，且集中在树的左侧。
- 叶节点只可能在层次最大的两层上出现。
- 任一节点，若右分支的子孙的最大层次为 $l$，则左分支下的子孙的最大层次必为 $l$ 或 $l+1$。

具有 $n$ 个结点的完全二叉树的深度公式是什么？ :: $\lfloor \log_2 n \rfloor + 1$<br><br>**附：证明过程（无需背诵，理解即可）**<br>设完全二叉树的深度为 $k$。<br>根据二叉树第二条性质得：$2^{k-1} - 1 < n \le 2^k - 1$<br>即：$k-1 \le \log_2 n < k$<br>因为 $k$ 只能是整数，因此 $k = \lfloor \log_2 n \rfloor + 1$