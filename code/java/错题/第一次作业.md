#flashcards/Java

Java 中像 `import java.util.Scanner;` 这样精确到具体类的导入语句，被称为哪种导入方式？ :: 特定导入。

Java 中如何使用通配符导入 `java.util` 包下的所有类（请写出完整语句，注意标点）？ :: `import java.util.*;`

假设已初始化 `Scanner` 对象（例如命名为 `input`），若要读取用户输入的一个 `int` 类型的数据，应调用它的哪个方法（请带上括号）？ :: `nextInt()`